# Tank Battle 改进计划 (Enhancement Plan)

> 目标：在「可玩」的基础上，把坦克大战打磨成更具深度、可重玩（replay）与可维护的作品。
> 优先级说明：🔴 阻塞/高危 · 🟡 体验核心 · 🟢 长期打磨。

---

## 阶段 1 · 打磨可玩性（🔴 高优先级）

1. **敌人 AI 逻辑增强**
   - 让敌人朝基地玩家移动时带方向倾向（现在纯随机），降低「原地打转」。
   - 加入简单的「避免卡墙回退」：同方向撞墙 >N 帧则换方向。

2. **出生碰撞健壮性**
   - 当前有 `Wall 12 存在，2 层` 的提示，说明出生时可能短暂与墙体/边界重叠。
   - 增加多次重试，并把出生点选在玩家坦克附近更安全处。

3. **关卡数据驱动化**
   - 将关卡地图（砖/钢/空地）改为外部数据或更清晰的层结构，方便多关卡扩展。
   - 建议：`levels/` 目录存字符串地图，`Game` 读取第 N 关。

---

## 阶段 2 · 玩法深度（🟡 中优先级）

4. **敌人类型分层**
   - 普通/快速/重装坦克，不同速度与血量，击杀得分不同。

5. **道具/能力（Power-ups）**
   - 击杀敌人时随机掉落，可拾取后强化坦克：增加血量、快速射击、临时护盾、地图探测等。

6. **基地增强逻辑**
   - 基地被攻击时给予更明显的受击反馈（闪烁/音效），并支持多座基地增加难度。

---

## 阶段 3 · 体验与打磨（🟢 长期）

7. **音效与音乐**
   - 射击、爆炸、选中、过关、失败等音效 + 背景音乐（可开关）。

8. **UI/界面**
   - 开始菜单、分数榜/记录、暂停菜单更友好、关卡选择。
   - 游戏内 HUD 优化：当前关卡、剩余敌人、玩家血量。

9. **存档与成就（可选）**
   - 记录最高分/进度；轻量成就系统。

---

## 阶段 4 · 工程化（🟢 长期）

10. **代码结构分层**
    - 将 `tank_battle.py` 较大类/方法按职责拆分：`entities/`（坦克/子弹/基地/地图）、`screen/`、`state/`。
    - 降低单文件复杂度、提高可测性。

11. **持续自动化测试**
    - 在 `pytest` 基础上增加 AI 路径、道具、持久化等单元/集成测试。

12. **性能优化**
    - 用 `pygame.time` 计时器统一管理事件，避免主循环 `Clock.tick` 占用。
    - 大地图下做空间分区碰撞检测，减少每帧全量遍历。

---

## 里程碑建议

- **M1**：完成阶段 1（出生点 + 敌 AI 轻量 + 关卡数据化）。
- **M2**：完成阶段 2（敌人层 + 道具 + 基地受击反馈）。
- **M3**：完成阶段 3（音效 + UI）。
- **M4**：完成阶段 4（代码分层 + 持续测试）。

> 建议每阶段先写一条 `chore` 或 `feat` 提交，保持改动可追溯。
